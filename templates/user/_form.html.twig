{{ form_start(form) }}
<div id="containerProfil">
	<div class="form-group">
		<label>Nom</label>
		{{ form_widget(form.firstName, {'attr': {'class': 'form-control'}}) }}
	</div>
	<div class="form-group">
		<label>Prénom</label>
		{{ form_widget(form.lastName, {'attr': {'class': 'form-control'}}) }}
	</div>
	<div class="form-group">
		<label>Email</label>
		{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
	</div>
	<div class="form-group" id="form-group-password-user">
		{{ form_label(form.password) }}
		{{ form_widget(form.password, {'attr': {'class': 'form-control', 'type': 'password', 'id': 'form_password'}}) }}
	</div>
</div>
<div id="btnValidateEditProfil">
	<button class="button">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
